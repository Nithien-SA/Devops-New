# Mock IoT Data Generator (FastAPI)

This is a simple FastAPI app that simulates IoT sensor data such as temperature, humidity, and light levels.

## Run Locally

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
